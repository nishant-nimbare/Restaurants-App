package com.nishantnimbare.unicodeproject.Util;

//this interface is used as a callback to notify the mainActivity from adapter that a recyclerview item was clicked
public interface ItemClickListener {
     void onItemClicked(int position);
}
